global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "journal-app"
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["journal-app:8080"]
